# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Dependencies (in case a build step is added later)
node_modules/

# Build output / artifacts
dist/
build/
.cache/
.tmp/

# Environment / secrets
.env
.env.*
!.env.example

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# Server-side secrets — created directly on the server, never committed
api/config.php
